<!DOCTYPE html>
<html>
    <head>
        <title>Date</title>
    </head>
    <body>
        <h1>Today</h1>

        <p>
          <% d = Date.today %>
          <%= "The date is #{d.strftime('%A, %B %d, %Y')}" %>
        </p>

    </body>
</html>
